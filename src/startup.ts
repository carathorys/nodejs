import { ConfigurationModel } from './models';

export class Startup {
  constructor(private configuration: ConfigurationModel) {}

  public Services(): void {
    // TODO: Set up service scopes
  }
}
